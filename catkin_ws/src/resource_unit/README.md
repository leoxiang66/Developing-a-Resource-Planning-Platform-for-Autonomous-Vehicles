# resource_unit

Dependencies:
- matplotlib
- ros melodic

Before running you have to compile the package:
`catkin_make`

After all running the perception and localization ru the following command:

`roslaunch resource_unit resource_monitor.launch`
